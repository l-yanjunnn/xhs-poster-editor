# v1.12.0-rc.1 本地候选说明

2026-09-14，北京时间。基于 v1.11.0 / `2c98247`，仅本地审阅，未发布。

长封面不再压缩文字块；超出画布、发生重叠或检测到字体/字形异常时阻断导出。成品预览展示实际生成的 PNG，下载复用同一批文件；内容或样式变动后旧成品失效。导出固定样式和资源版本，只克隆目标页，并检查最终几何、绘制字体与可观测的字形墨迹。

真实后退缓存恢复时重新获得写锁并读取最新保存内容；数据库事务另行拒绝旧版本覆盖，保存冲突恢复副本。素材、字体和主题以事务完成为成功依据，失败可重试。中文粗体边界保留用户输入的空格，Markdown 代码围栏内标题不会被提取为封面标题。

新增连续空格显示模式、超级紧凑密度、封面顶部偏移、内页逐页上/中/下位置。旧稿缺省继续原显示规则，旧密度值不变；逐页配置跟随稳定分页身份，支持撤销重做与保存恢复。界面使用系统字体，海报继续使用所选字体。可本地下载诊断，默认不含原文，显式选择后才附当前文档。

验证：类型检查、lint、47 文件/488 单测通过；macOS Chrome 153 的正式构建完成桌面/1280px、键盘/焦点/reduced-motion、真实 BFCache、事务中止、字体失败恢复、三主题和自定义字体、12 组字体/封面矩阵、54 组长封面、分页续写、代码块、19 页 ZIP、预览/下载字节一致及真实目录流失败续传。安全两页旧稿样例与基线的预览像素完全一致。

限制：周宇原始九页稿及异常 PNG 仍缺失，Z5/Z6 未关闭；没有认定磁吸为根因。原作者浏览器、Safari/Edge/Windows、线上双入口及原生系统目录弹窗未完成验证；真实目录接口与 ZIP UI 已验证。像素门禁覆盖可观测的不透明核心墨迹，不宣称所有字形逐像素认证。旧版已经删除的空格无法自动恢复。

完整证据与本地构建位置：[本轮交付报告](/Users/a0000/Codex_YJ/tasks/xhs-editor-20260914/REPORT.md)。不要将本候选说明用作已上线公告。
