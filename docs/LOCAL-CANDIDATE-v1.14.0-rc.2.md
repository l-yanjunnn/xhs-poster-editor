# v1.14.0-rc.2 本地候选：封面可见范围外右上角

在 rc.1 的两档选择基础上，仅新增「封面可见范围外右上角」。沿用同一裁切上边界常量，Logo 顶部 = 裁切上边界 − Logo 尺寸 − 5 px；当前为 180 − 120 − 5 = 55 px。右侧距离与尺寸不变，完整 PNG 保留品牌；中心 3:4 参考可见区内不露出。旧数据默认仍是封面可见区右上角。

用户已确认继续手动选页：滚动不改变设置目标，在「页面布局 → 设置当前页」选择后再修改本页 Logo。相关代码未改，USAGE.md 已说明；未更新外部使用说明。

- 路径：`/Users/a0000/Codex_YJ/tasks/xhs-logo-20260917/candidate`
- 分支：`codex/xhs-logo-20260917`；本次起点 `d8b1d8614d0f1300902ef335e8fe9ac1b73a7f3a`，最初基线 `1cd7d330065be0eb330c69625aa8a045ee2b3c53`；正式版本仍 v1.13.0。
- 预览：http://127.0.0.1:4198/ 。任务目录 `START-LOCAL.command` 可启动；修改源码后在 candidate/app 执行 `pnpm build` 再启动。当前 dist 已构建为 rc.2。
- 检查：49 文件 / 517 单测、ESLint、TypeScript 与 Vite build 全通过，git diff --check 通过。
- Chrome 153 隔离合成稿：三档几何为 top 200 / 100 / 55 px；右距60、尺寸120不变，内页不变；隐藏再显示、撤销重做、刷新、键盘选择、主题保存/缩略图/重新应用、第二页覆盖保留通过。
- 三档各3页，共9张真实下载PNG，均2160×3600，每张与同批成品预览逐字节一致。新位置底边为导出像素350，裁切上沿360，相差10导出像素，即5画布像素。实际截图和PNG已目检。
- 900 / 600 / 390 px：沿用既有1280px工作区，Logo卡片内部无横向溢出，无新增页面宽度。
- 证据：任务目录 `evidence/rc2/build-review.json`、`geometry.json`、`review-outside.png`、三档 `review-*.png`、三份ZIP及9张实际PNG。可重跑 `python3 tools/logo-candidate/outside-review.py`。

本轮没有推送、合并、部署或发送消息。原仓库与 rc.1 证据保留。公考主题原有隐藏约束继续生效。中心裁切仅为编辑器模拟，本地合成稿通过不等于真实平台或生产验收；等待用户实际审阅后再接手发布。
