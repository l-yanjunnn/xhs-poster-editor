# v1.10.0–v1.10.2 合并发布说明

> 2026-08-16，v1.10.2 已完成 Cloudflare 和大陆通道双轨发布。本文合并记录 v1.10.0、v1.10.1 和 v1.10.2 的改动，并附上一份可直接发送的公告。

## 这三个版本改了什么

### v1.10.0：公考封面版式

- 「公考·山水卷」首图新增左对齐叠排、居中海报、小字在上大字在下三种版式，每种都可选上、中、下三档垂直位置。
- 版式只改封面主标题和副标题的画布呈现，不改正文结构，也不影响内页。旧主题和旧草稿缺少新字段时，仍按原来的左对齐上方版式显示。
- 未修改的默认教程切换到公考主题时，首页会换成对应的示例封面，这一步可以撤销。用户改过教程内容后，系统不会覆盖原文。

### v1.10.1：删光草稿后恢复教程

删除最后一份草稿后，编辑器现在会重新建立完整的 5 页使用教程，而不是留下一个空白画布。教程使用默认雅致样式，刷新页面后仍然保留。

### v1.10.2：弹窗、续段和 Code 块修复

- 导入和导出弹窗适配较矮的浏览器窗口。标题与底部操作按钮保持可见，结果区单独滚动。
- 在段落中插入分页，或按 Enter 后紧接着插入分页时，上一页的续段末行会正常铺满版心。真正的段落末行仍保持自然左对齐。
- Code 块中的长中文、URL 和无断点 token 会在固定画布内换行，空格、Tab、空行和手工换行保持不变。编辑区、预览与 PNG 导出结果一致。

同一标题内换行仍使用 Shift+Enter；Enter 表示新建段落。Code 块的本次修复针对横向超宽，如果整块内容在垂直方向超出一页，仍需手动插入分页。

## 发布与验证

- 当前版本：`v1.10.2`
- 发布提交/tag：`0b2d6468c9f8aa4db9cdf6d88533b7eaff47f267` / `v1.10.2`
- Cloudflare：https://xhs-poster-editor.l-yanjunnn.workers.dev
- 大陆通道：https://xhsposter.tshzchen.cn

两个生产入口加载同一构建：

| 文件 | SHA-256 |
|---|---|
| `index.html` | `2528734a5922cd8f3947cc14edb405d1631f51bfbdaecb20aca060fbd0c65204` |
| `assets/index-Bdq1zB4M.js` | `c0556c0125cd759c9277c2d1ed3f73767c96304389fa0c578f1bf3ae48f93368` |
| `assets/index-BosMHSUQ.css` | `cfbf5ce9fc0a564b264242f65ab017de76191f630ceb9564c5e1b002654bda36` |

本地类型检查、Vitest（45 个文件 / 465 项测试）、ESLint 和生产构建全部通过。两个生产入口分别跑完深度回归、低视口弹窗、跨页续段和 Code 块换行回归。

`archive/dist-v1.10.2/` 已建立，大小为 9.6M。归档按项目约定排除 3337 个字体文件，完整复原时从 `v1.10.2` tag 运行 `bash ci.sh` 重建。

## 可直接发送的合并公告

> 小红书排版编辑器已更新到 v1.10.2。这条公告合并说明 v1.10.0–v1.10.2 的改动。
>
> 「公考·山水卷」封面现在有三种版式：左对齐叠排、居中海报、小字在上大字在下，同时可选上、中、下三档位置。版式只影响首图，不会改动正文和内页。
>
> 草稿恢复也做了调整。删除最后一份草稿后，编辑器会回到完整的 5 页使用教程，不再显示空白画布。
>
> 这次还修复了三个日常使用问题：较矮窗口中的导入/导出按钮不再被遮住；正文跨页续段会正确铺满，真段尾仍保持自然左对齐；Code 块里的长中文、URL 和连续长串会自动换行，不再被画布右侧裁掉。
>
> 使用时请注意：同一标题内换行用 Shift+Enter，Enter 会开始新段落。Code 块如果高度超过一页，请手动插入分页。
>
> Cloudflare：https://xhs-poster-editor.l-yanjunnn.workers.dev
>
> 大陆通道：https://xhsposter.tshzchen.cn
>
> 草稿、发布文案、素材和字体只保存在当前浏览器。两个域名的本地数据互不相通，平时从哪个入口编辑，建议继续使用同一个入口。

这份公告交由用户自行发送，未通过项目机器人代发。
