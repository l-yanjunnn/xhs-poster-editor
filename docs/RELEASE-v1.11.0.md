# v1.11.0 发布说明

> 状态：**发布就绪，双轨部署前**。2026-08-19 已完成本地实现、视觉值锁定、版本 bump 和最终本地回归；Cloudflare / 大陆通道的部署与生产资源同构验证将在上线后以真实证据补齐。

## 这次改了什么

### 封面副标题新增三档微排版

右侧「封面版式」新增「副标题字距」，可选紧凑、标准、舒展。它只影响首图中紧跟第一个 H1 的封面副标题，不改变主标题、内页正文或其他段落。

经三种封面版式的画布与 2160×3600 PNG 目检，本版正式锁定以下值：

| 封面版式 | 紧凑 | 标准 | 舒展 |
|---|---:|---:|---:|
| 左对齐叠排 / 居中海报 | `0em` | 不新增覆盖，继承 v1.10.2 | `0.08em` |
| 小字在上大字在下（kicker） | `0.10em` | 不新增覆盖，继承 v1.10.2 的 `0.18em` | `0.26em` |

标准档是兼容基线：三种版式的 sealed geometry、snapshot 和画布截图与 v1.10.2 real-fixture 严格相同；2160×3600 PNG 只有 48–98 个像素的 1 色阶渲染抗锯齿差异（最高 0.00127%，低于 0.002% 门槛）。紧凑与舒展只覆盖副标题字距，不另行改变字号、宽度、位置或文本内容。本地 3 版式 × 3 档证据见 [`screenshots/v1.11.0/`](screenshots/v1.11.0/)。

### 「全篇 H1 宽度」命名澄清

右栏原「H1 宽度」改名为「全篇 H1 宽度」，明确它作用于全文所有 H1。此次只改标签文案：`h1Width` 行为不变，左侧工具栏的 H1 编辑功能也没有移动。

## 兼容与边界

- 新建草稿与主题默认使用标准档。
- 旧 V1/V2 草稿、旧内置主题和旧自定义主题缺少字段或字段无效时，统一回落到标准档，保持 v1.10.2 原视觉。
- 有意输入的普通空格、NBSP 与 Shift+Enter 软换行会按原文保存；切换三档不改写副标题文本。
- 本版不新增副标题宽度、自由滑杆、局部字距、自由文本框，也不扩展到内页。
- 本版不改确定性排版算法、分页、安全区，以及 PNG / ZIP / 目录导出算法；导出路径只消费与预览相同的封面样式状态。

## 发布与验证

- 目标版本：`v1.11.0`
- 发布提交/tag：`TBD_RELEASE_SHA` / `v1.11.0`
- 发布闭环提交：`TBD_CLOSURE_SHA`
- Cloudflare：https://xhs-poster-editor.l-yanjunnn.workers.dev
- 大陆通道：https://xhsposter.tshzchen.cn
- 本地发布构建：`index-CStU0xGT.js` / `index-gRCFVCH5.css`；SHA-256 为 `cf9da655b9a2e70f060b84123fdc1d5b987e4e645e7706d4889b31640e76daf6` / `1dc5e06d171ae90fd8e8ff91d05107d2dcfc23407799383ba88f4c7dfb816cac`
- 生产静态资源与 SHA-256：`TBD_ASSETS`
- 最终本地门禁：`tsc -b`、ESLint、Vite build 全绿；Vitest **45 文件 / 481 测试**；真实 Chrome 状态链 10/10、三主题 2160×3600 PNG 深回归、公考 3 版式×3 档全尺寸矩阵全绿
- 双入口生产回归：`TBD_PRODUCTION_GATES`
- 不覆盖归档：`TBD_ARCHIVE`
- 公告：专业文案已准备；未获得「代发」授权，不调用飞书机器人、不伪造回执 ID

已完成的本地候选证据包括三种版式 × 三档的真实 Chrome 画布截图、9 张 2160×3600 PNG、sealed geometry manifest，以及 standard 与 v1.10.2 权威基线的严格几何/截图比较和 PNG 抗锯齿容差比较。最终发布记录只会填写实际提交、资源哈希、回归结果与归档，不以占位符冒充上线证据。

## 发版公告草稿

> 【发版公告】小红书排版编辑器 v1.11.0 上线
>
> 本次新增「封面副标题微排版」。在右侧「封面版式」中，可以为首图副标题选择紧凑、标准或舒展三档字距；标准档完整保留旧版视觉，旧草稿和旧主题也会默认按标准档打开。
>
> 右栏「H1 宽度」同时改名为「全篇 H1 宽度」，用于说明它仍作用于全文所有 H1；功能行为没有变化，左侧 H1 编辑入口也没有移动。
>
> 使用边界：三档只影响封面副标题，不改变主标题、正文、分页和安全区，也没有新增自由滑杆、副标题宽度或自由文本框。
>
> 🌐 工具地址
>
> · Cloudflare：https://xhs-poster-editor.l-yanjunnn.workers.dev
> · 大陆通道：https://xhsposter.tshzchen.cn
>
> ✅ 上线状态
>
> Cloudflare 与大陆通道已完成同构部署和真实 Chrome / PNG 生产回归。

双轨部署和生产回归通过后，本公告作为可直接发送的交付文案；只在用户明确要求代发时才调用飞书机器人。
