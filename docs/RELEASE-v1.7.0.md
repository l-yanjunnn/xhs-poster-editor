# 小红书排版编辑器 v1.7.0 正式发布说明｜导入文稿与完整导出

> 状态：已于 2026-08-11 正式双轨上线。发布提交/tag 为 `87a2d89` / `v1.7.0`；Cloudflare、OSS/CDN、本地构建与归档核心资源一致，生产回归已通过。

- Cloudflare：https://xhs-poster-editor.l-yanjunnn.workers.dev
- 大陆通道：https://xhsposter.tshzchen.cn

## 本版重点

1. **导入 Markdown / 纯文本**：顶栏新增「导入文稿」，支持选择文件、拖放和粘贴全文。先确认结构、页数、封面与发布文案，再生成到一个独立新草稿
2. **生成后继续手动调整**：导入结果进入原有 Tiptap 编辑、撤销和自动保存链，不是只读快照；正文、标题、分页和右栏发布文案都可继续修改
3. **发布文案独立保存**：专用文稿中 `# 正文` 以下内容放在右栏独立卡片，不进入图片正文，不随导出页码复制，也不添加“上篇 / 下篇”
4. **精确处理 17 / 18 / 19+ 张**：18 张是普通图文单篇上传兼容线，不是编辑器生成或导出上限；19+ 仍完整生成到同一草稿
5. **19+ 可以一次完整导出**：全部导出前增加一次本地留存确认，明确完整图片不能作为一篇普通图文一次上传，但不会禁止导出
6. **自选页码**：同时保留范围输入与缩略图多选，二者双向同步；导出文件始终使用原稿页码，升序去重
7. **独立文件夹 + 兼容 ZIP**：支持时选择父目录并创建一个独立子文件夹；不支持目录写入时回退为一个 ZIP。两种方式都不按 18 张分包
8. **中文正文两端对齐**：左侧编辑区和 1080×1800 成品画布正文保持两端对齐，段尾自然靠左；H1/H2/H3 不强制拉伸

## 状态文案

- 1–17 张：`可作为一篇普通图文发布`
- 18 张：`18 张，达到当前普通图文单篇上限`
- 19+ 张：`共 N 张，超过普通图文单篇上限 18 张；仍会完整生成`

18 由 `app/src/lib/productConfig.ts` 集中配置，只表示当前普通图文单篇的高置信兼容上限；不代表小红书所有内容类型的统一限制。原生长文是另一条平台管线，本版没有接入。

## 导出交付规则

- 图片：`01_[文档主题]_cover.png`、`02_[文档主题]_inner.png`，页码至少补两位
- 文件夹：包含主题、导出范围、张数和北京时间；重复导出自动追加 `-02` / `-03`
- 清单：每次附带 `导出清单.json`，记录源页、文件名、交付方式和普通图文兼容判断；发布文案不进入导出包
- 目录：内部逐页生成并写入，文件成功关闭后才记为完成；中断后可在同一页面会话内继续剩余页
- ZIP：只有一个顶层文件夹，不拆成多个生产包；支持原生“另存为”的浏览器可现场修改 ZIP 名称和位置，否则使用浏览器下载

## 验证与生产证据

- Vitest：37 文件 / 292 测试通过
- TypeScript、ESLint、Vite 生产构建通过
- 本地真实点击：18 页真实文稿、19 页超限文稿、手动编辑与自动保存、全部导出二次确认、自选页码、缩略图同步和越界失败状态均通过
- 两个生产入口分别真实下载约 196 MB 的 19 页兼容 ZIP；唯一顶层目录，PNG `01..19` 无丢失或重复且均为 2160×3600，导出清单 `sourcePages=1..19`
- 两个生产入口的 1/2/5 页、列表分页、三旧主题、用户字体、2160×3600 像素、字形中线、撤销重做与标题边界回归均通过
- Cloudflare 生产页面完成一次原生目录写入：一个独立文件夹包含 5 张 2160×3600 PNG 和最后写入的 `导出清单.json`；清单为 `deliveryMode=directory`、`sourcePages=1..5`
- 生产资源：Cloudflare 与大陆通道均加载 `assets/index-BKYNhwYN.js` / `assets/index-BVsea9B1.css`；JS SHA-256 为 `fbfafa1ec17407fc9886b7d7e74977d7622324275106949219d9c3b202b60dfc`，CSS SHA-256 为 `39a1a1dbc10c2debdfb209ad5b68b42ee686a9bfb9cfa62563627fb3bcd71c13`，与本地及归档一致
- 页码断言：17/18/19+ 边界、连续/不连续选择、原稿编号、无丢页、无重复、单文件夹/单 ZIP、中断续写均覆盖
- 版式：正文两端对齐、段尾靠左、标题自然左对齐；列表、引用、粗体和长段落已在实际 1080×1800 画布目检
- 控制台：两个生产入口的 console error / page error 均为 0

截图保存在 `docs/screenshots/v1.7.0/`。

## 兼容与未纳入范围

- 旧草稿、旧主题、素材、字体与现有 2160×3600 渲染链保持兼容
- 未混入 v1.6 字体本地化、字体减重、字重档位或结构化封面
- 未接入原生长文，也未实现 37+ 上传分组整体重均衡
- 目录中断续写当前只保证同一页面会话，刷新后的跨会话恢复留待后续
- 兼容 ZIP 需要先在浏览器内存中汇总；支持目录写入时默认推荐逐页写入独立文件夹，尤其适合超多页文稿
- 若回滚至 v1.5.1 后重新编辑导入草稿，旧代码可能丢失可选 `publication` 元数据，图片正文仍保留；这是本次已接受并记录的窄回滚权衡

## 发布记录

- 发布提交/tag：`87a2d89` / `v1.7.0`
- 双轨：Cloudflare Workers 与阿里云 OSS/CDN 均已上线
- 归档：`archive/dist-v1.7.0/`，9.5M；排除字体 3337 个并记录于 `FONTS-MANIFEST.txt`
- 飞书：旧企业租户 `default` 的「Claude聊天助手」已向刘彦君既有发布会话发送正式公告，API 回执消息为 `om_x100b6887b2a35ca8b4a99575b534996`
- 后续应用修复必须使用新版本号，不移动 `v1.7.0` tag
