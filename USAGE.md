# 小红书排版编辑器 使用说明

> 本说明已更新至本地生产候选 **v1.7.0**。当前线上仍为 v1.5.1；待用户目检确认后再双轨发布。

## 怎么打开

浏览器访问：

- Cloudflare：**https://xhs-poster-editor.l-yanjunnn.workers.dev**
- 大陆通道：**https://xhsposter.tshzchen.cn**

无需安装、无需注册，Chrome / Edge / Safari 都行。打开就是一份 5 页的「使用教程」样张，照着看一遍就会用了。

## 基本流程

1. **左侧写正文**：直接输入或粘贴，两行工具栏里的 H1 / H2 / H3 / 引用 / 列表 / 加粗 / 下划线 / 分隔线 / 分页 / 图片等功能都直接可见；「↓ 插入分页 ↓」会把后续内容另起一页
2. **中央看成品**：9:15（3:5）画布实时排版，多页竖排展示；图片的直接操作都在这里完成
3. **右侧调属性**：未选对象时显示页面与主题；选中文字时显示文字排版与荧光笔；选中图片时显示该图片的对齐、宽度、替换和删除
4. **顶部做全局操作**：撤销 / 重做、草稿状态、导入文稿、裁切参考、排版参考、磁吸和导出集中在顶部
5. **换素材与字体**：「素材库」上传自己的背景图 / Logo / 图片；「字体库」拖入 ttf / otf / woff 等字体文件
6. **保存草稿**：正文、样式、背景和 Logo 自动保存在当前浏览器；点「草稿」可另存、切换或删除。主题只保存可复用的样式
7. **导出**：点唯一的紫色主按钮「导出 PNG」→ 选择全部或自选页码 → 选择独立文件夹或兼容 ZIP → 确认主题与名称。所有 PNG 均为 2160×3600

## 小技巧

- **导入后仍可手动调整**：点顶栏「导入文稿」，选择 `.md` / `.txt` 或粘贴全文；确认解析后会新建一个普通草稿。自动编排不是只读结果，正文、标题、分页和右栏发布文案都能继续修改并自动保存
- **专用文稿结构**：使用 `# 封面` 与 `# 正文` 分开图片正文和发布文案。普通文稿里的 `---` 会在导入时一次性询问：全文统一作为分页，或统一保留为普通分隔线
- **18 张只是上传兼容线**：1–17 张显示“可作为一篇普通图文发布”；18 张显示“18 张，达到当前普通图文单篇上限”；19+ 仍会完整生成到同一草稿，不截断、不缩字、不改写
- **19+ 可以一次导出**：选择“导出全部”时会增加一次本地留存确认，提醒完整图片不能作为一篇普通图文一次上传；它不会限制编辑器一次性导出
- **也可以自选页码**：范围输入支持 `1-3, 5, 19`，并与缩略图点选双向同步；导出的文件仍使用原稿页码，避免顺序错乱
- **目录与 ZIP 回退**：支持目录写入的浏览器会让你选择父目录，并自动创建一个独立子文件夹；超多页优先用目录模式，逐页落盘更节省浏览器内存。不支持时使用一个兼容 ZIP，ZIP 内也只有一个顶层文件夹，不按 18 张拆包，仍可一次完整导出
- **导出命名**：图片使用 `01_文档主题_cover.png`、`02_文档主题_inner.png`；文件夹含主题、范围、张数和北京时间。重复导出自动追加 `-02` / `-03`，并附带 `导出清单.json`
- **中断后继续**：目录模式按页写入，单页成功关闭后才计为完成；同一页面会话内若中断，可只继续剩余页面，不重写已完成页
- **公考双底图主题**：在右侧选择「公考·山水卷」后，第 1 页自动用封面底图，第 2 页起自动用内页底图；增删分页时会按页序实时重算
- **封面与内页分开设置建议内容区**：公考封面按首图中心 3:4 可见区使用 `x=120…960 / y=300…1500`，内页保持更大的 `x=96…984 / y=180…1500`，二者没有统一；旧主题也不会被这次调整改变
- **封面主/副标题颜色**：公考主题的右侧页面设置中，可分别输入六位 HEX 色值；「恢复模板色」会回到主标题 `#6D136C`、副标题 `#5A465F`
- **列表内也能安全分页**：光标在无序或有序列表中时直接点「插入分页」，分页会成为真正的新页；有序列表的后半段会继续原序号
- **字形中线自动校准**：H2 前的竖线、有序列表的数字会根据当前字体和字号自动对齐，预览与导出无需手动微调
- **直接调图片**：在中央画布点击正文图片，拖四角手柄等比缩放；拖图片上方横向抓手可对齐到左 / 中 / 右。右侧也可直接选择对齐和常用宽度档位
- **磁吸与临时旁路**：拖动时会吸附内容边界、中心线和常用宽度；按住 Option / Alt 可暂时关闭磁吸，按 Esc 可取消本次拖动
- **三个开关互不影响**：「裁切参考」查看首图中心 3:4 可见区；「排版参考」显示版心边界和安全线；「磁吸」只辅助图片操作
- **辅助层不进成品**：裁切遮罩、参考线、图片选框、手柄和吸附线都只在编辑界面显示，导出时自动剥离
- **短语不拆行**：选中 1–12 个字符后点「短语不拆」，适合机构名、日期和短关键词；编辑器里的蓝色虚线提示不会进入成品
- **正文荧光笔**：先选中文字，再在右侧打开荧光笔；颜色固定为 `#7B3B8B`，透明度可在 0%–100% 连续调整，默认 50%
- **只开一个编辑页**：同一浏览器若已在另一个标签页编辑，本页会暂时只读；关闭另一页后会自动载入最新草稿再继续
- **标题作用于整段**：工具支持 H1 / H2 / H3。Enter 会新建段落，Shift+Enter 只在当前段内换行；同一段里的自动换行或 Shift+Enter 内容会一起使用当前标题样式
- **失焦后也能撤销**：点过顶栏或画布后仍可使用 Command/Ctrl+Z；重做使用 Shift+Command/Ctrl+Z，Windows 也支持 Ctrl+Y。普通输入框、弹窗、组合输入和图片拖动过程会保留自己的交互，不会被全局快捷键抢走
- **一次手势只记一次**：一次图片拖动或缩放只产生一条撤销记录；按 Esc 可取消当前手势
- **资源失败可恢复**：某张图片、字体或主题资源加载失败时，只降级该资源，并在原位置提供原因和重试，不会清空正文
- **导出前检查**：资源仍在加载时会等待；若有失败项，会明确提示重试或继续导出

## 数据存哪儿

你的草稿、导入文稿的独立发布文案、上传素材、字体和自定义主题**只存在当前浏览器里**（IndexedDB），不上传服务器。v1.4 / v1.5 旧草稿打开后会继续兼容，不会被统一改成公考主题。输入后即使马上关页也有临时恢复保护；但换浏览器、换域名或清缓存仍会看不到原数据，重要原图和导出结果请自己另存一份。
