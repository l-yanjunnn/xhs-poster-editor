# 小红书排版编辑器 使用说明

> 本说明对应本地 **v1.3.0 候选版**。线上当前仍是 v1.2.0，待视觉确认后再发布。

## 怎么打开

浏览器访问：**https://xhs-poster-editor.l-yanjunnn.workers.dev**

无需安装、无需注册，Chrome / Edge / Safari 都行。打开就是一份 5 页的「使用教程」样张，照着看一遍就会用了。

## 基本流程

1. **左边写文字**：直接输入或粘贴，用编辑区上方按钮切 H1 / H2 / H3 / 引用 / 列表 / 加粗 / 下划线，「↓ 插入分页 ↓」另起一页，还能插入图片和分隔线
2. **右边实时预览**：9:15（3:5）画布实时排版，多页竖排展示
3. **顶部选样式**：主题（雅致 / 极简白 / 深夜黑）、字体（标题正文分开选）、字号、间距、叠色、Logo 显示策略、H1 宽度
4. **换素材**：「素材库」上传你自己的背景图 / Logo；「字体库」拖入你自己的字体文件（ttf/otf/woff 等）
5. **保存草稿**：正文、样式、背景和 Logo 自动保存在当前浏览器；点「草稿」可另存、切换或删除。主题只保存可复用的样式
6. **导出**：「导出 PNG」→ 填文件名 → 单页直接下载 2160×3600 PNG，多页打包成 zip

## 小技巧

- **首图裁切参考**：所有页面仍导出为 9:15；第一页可打开「裁切参考」，查看平台中心裁成 3:4 后的可见区
- **参考线不进成品**：上下变暗、橙色裁切线和蓝色内容边界都只在预览显示，导出时自动剥离
- **短语不拆行**：选中 1–12 个字符后点「短语不拆」，适合机构名、日期和短关键词；编辑器里的蓝色虚线提示不会进入成品
- **只开一个编辑页**：同一浏览器若已在另一个标签页编辑，本页会暂时只读；关闭另一页后会自动载入最新草稿再继续
- **标题内换行**：Shift+Enter 强制换行不另起段
- **图片大小**：选中图片后用顶栏「图片宽度」调整（原大小 / 33% / 50% / 75% / 100%）
- **撤销**：编辑区里 Cmd+Z

## 数据存哪儿

你的草稿、上传素材、字体和自定义主题**只存在当前浏览器里**（IndexedDB），不上传服务器。输入后即使马上关页也有临时恢复保护；但换浏览器、换域名或清缓存仍会看不到原数据，重要原图请自己另存一份。
